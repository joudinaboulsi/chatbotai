# AI Chatbot Platform — Admin Frontend

React + TypeScript + Vite + Tailwind admin panel. See the repository root `README.md` for full setup, deployment, and API documentation.

```bash
npm install
npm run dev      # http://localhost:5173, proxies /api, /media, /widget.js to http://localhost:8000
npm run build    # production build -> dist/
```
